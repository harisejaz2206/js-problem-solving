// Question 37: `Object.keys` and `filter` - Filtering Object Properties
// Given an object with various settings, use `Object.keys` and `filter` to create a new object containing only the settings that are enabled (`true`).

const settings = {
    darkMode: true,
    notifications: false,
    autoSave: true,
    locationAccess: false
  };
  // Your code here
  