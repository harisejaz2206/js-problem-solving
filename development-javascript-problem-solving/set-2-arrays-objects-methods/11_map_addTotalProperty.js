// Question 11: `map` - Calculating Derived Values
// Given an array of objects with `price` and `quantity`, use `map` to add a new property `total` for each object, representing the product of `price` and `quantity`.

const items = [
    { price: 10, quantity: 2 },
    { price: 5, quantity: 5 },
    { price: 8, quantity: 3 }
  ];
  // Your code here
  