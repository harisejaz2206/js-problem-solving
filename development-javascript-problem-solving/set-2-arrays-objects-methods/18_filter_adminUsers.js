// Question 18: `filter` - Filtering Nested Arrays
// Given an array of user objects, each with an array of roles, use `filter` to return users who have the role `"admin"`.

const users = [
    { name: "Alice", roles: ["user", "admin"] },
    { name: "Bob", roles: ["user"] },
    { name: "Charlie", roles: ["user", "moderator"] }
  ];
  // Your code here
  