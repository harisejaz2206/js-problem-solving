// Question 45: `some` with `Object.values` - Using `some` with `Object.values`
// Given an object representing inventory with product names as keys and quantities as values, use `some` to check if any product is out of stock (`quantity === 0`).

const inventory = {
    "Laptop": 5,
    "Phone": 0,
    "Tablet": 3
  };
  // Your code here
  