// Question 30: `forEach` - Combining `forEach` with `map`
// Given an array of students, use `forEach` to collect all the scores into a separate array, then use `map` to calculate the average score.

const students = [
    { name: "Alice", scores: [80, 90, 100] },
    { name: "Bob", scores: [70, 85, 95] },
    { name: "Charlie", scores: [60, 75, 85] }
  ];
  // Your code here
  